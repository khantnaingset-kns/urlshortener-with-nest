export interface ShortenURLPack {
  urlCode: string;
  shortenURL: string;
  expireDate: string;
  version: string;
}

export interface OriginalURLPack {
  originalURL: string;
  expireDate: string;
}
